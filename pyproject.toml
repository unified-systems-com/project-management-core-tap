# Identity chain (pre-boot conformance gate enforces agreement):
#   slug = "project_management_core"  ·  distribution = "project-management-core-tap"  ·  import namespace = "tap_plugin.project_management_core"
#   entry-point key = "project_management_core"
[project]
name = "project-management-core-tap"
description = "Vendor-neutral project-management vocabulary substrate (milestones, tasks and the work that connects them). v0 is deliberately empty: the vocabulary is added as a real project needs it."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.14"
dynamic = ["version"]
dependencies = []

[project.entry-points."tap.plugins"]
project_management_core = "tap_plugin.project_management_core.apps:ProjectManagementCoreConfig"

# The plugin's OWN dev closure (PEP 735), so its suite runs in a bare checkout. Never installed by boot.
[dependency-groups]
dev = ["pytest>=8.3", "pytest-django>=4.9", "factory-boy>=3.3"]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "vcs"

[tool.hatch.version.raw-options]
fallback_version = "0.0.0"

[tool.hatch.build.targets.wheel]
only-include = ["tap_plugin/project_management_core"]
