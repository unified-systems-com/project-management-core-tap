{
  "version": 1,
  "description": "project_management_core CI record (req-boot-bootstrap-ci-record): the stack this plugin's own tests run in. Installs project_management_core alone (it declares no dependencies); the consumer flips self to editable so the checkout under test runs, not the pinned revision. Population is empty: v0 ships no GRIFT. Deliberately excluded: every other plugin, all credentials, anything that reaches the network.",
  "install": {
    "plugins": [
      {
        "slug": "project_management_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/project-management-core-tap",
          "rev": "6d7df6e81a35737272acbed275907042e5f21b52"
        },
        "note": "Self, pinned at the bootstrap commit until the first tagged release; the consumer (plugin-ci, or `spawn --dev-plugins project_management_core`) substitutes the checkout under test via tap.dev_workspace (req-boot-bootstrap-ci-record-4)."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": []
  }
}
